name: M2 Controller Preview QA

on:
  push:
    branches: [main]
    paths:
      - src/nightreign/input/night_controller_driver.gd
      - src/nightreign/input/night_controller_preview.gd
      - src/nightreign/input/night_controller_input.gd
      - src/nightreign/combat/night_attack_preview.gd
      - src/nightreign/expedition/night_overlay.gd
      - tests/night_controller_preview.gd
      - tests/night_controller_preview.tscn
      - tests/night_controller_input.gd
      - tests/night_controller_input.tscn
      - tests/night_attack_preview.gd
      - tests/night_attack_preview.tscn
      - tests/expedition.gd
      - tests/expedition.tscn
      - .github/workflows/m2-controller-preview-qa.yml
  pull_request:
    branches: [main]
    paths:
      - src/nightreign/input/night_controller_driver.gd
      - src/nightreign/input/night_controller_preview.gd
      - src/nightreign/input/night_controller_input.gd
      - src/nightreign/combat/night_attack_preview.gd
      - src/nightreign/expedition/night_overlay.gd
      - tests/night_controller_preview.gd
      - tests/night_controller_preview.tscn
      - tests/night_controller_input.gd
      - tests/night_controller_input.tscn
      - tests/night_attack_preview.gd
      - tests/night_attack_preview.tscn
      - tests/expedition.gd
      - tests/expedition.tscn
      - .github/workflows/m2-controller-preview-qa.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  controller-preview:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Godot 4.6.3
        run: |
          curl -L --fail --retry 3 -o /tmp/godot.zip https://github.com/godotengine/godot/releases/download/4.6.3-stable/Godot_v4.6.3-stable_linux.x86_64.zip
          unzip -q /tmp/godot.zip -d /tmp/godot
          chmod +x /tmp/godot/Godot_v4.6.3-stable_linux.x86_64
          /tmp/godot/Godot_v4.6.3-stable_linux.x86_64 --version

      - name: Clean import and reject script errors
        shell: bash
        run: |
          set -o pipefail
          rm -rf .godot
          /tmp/godot/Godot_v4.6.3-stable_linux.x86_64 --headless --editor --import --path . --quit 2>&1 | tee /tmp/m2-controller-preview-import.log
          ! grep -E "SCRIPT ERROR|Parse Error|Compile Error" /tmp/m2-controller-preview-import.log

      - name: Run controller preview contract
        shell: bash
        run: |
          set -o pipefail
          rm -f docs/M2_CONTROLLER_PREVIEW_QA.json
          /tmp/godot/Godot_v4.6.3-stable_linux.x86_64 --headless --path . res://tests/night_controller_preview.tscn --quit-after 1800 2>&1 | tee /tmp/m2-controller-preview.log
          ! grep -E "SCRIPT ERROR|Parse Error|Compile Error" /tmp/m2-controller-preview.log
          test -s docs/M2_CONTROLLER_PREVIEW_QA.json
          python3 - <<'PY'
          import json
          report = json.load(open('docs/M2_CONTROLLER_PREVIEW_QA.json', encoding='utf-8'))
          assert report.get('failures') == [], report.get('failures')
          assert report.get('checks', 0) >= 32, report.get('checks')
          assert report.get('controller_owned') is True, report.get('controller_owned')
          assert report.get('patterns') == ['single', 'sweep', 'thrust', 'burst', 'cast'], report.get('patterns')
          assert report.get('collision_clipped') is True, report.get('collision_clipped')
          assert report.get('turn_safe') is True, report.get('turn_safe')
          print('M2 controller preview QA:', report['checks'], 'checks passed')
          PY

      - name: Run controller regression
        shell: bash
        run: |
          set -o pipefail
          rm -f docs/M2_CONTROLLER_QA.json
          /tmp/godot/Godot_v4.6.3-stable_linux.x86_64 --headless --path . res://tests/night_controller_input.tscn --quit-after 1800 2>&1 | tee /tmp/m2-controller-regression.log
          ! grep -E "SCRIPT ERROR|Parse Error|Compile Error" /tmp/m2-controller-regression.log
          test -s docs/M2_CONTROLLER_QA.json
          python3 - <<'PY'
          import json
          report = json.load(open('docs/M2_CONTROLLER_QA.json', encoding='utf-8'))
          assert report.get('failures') == [], report.get('failures')
          assert report.get('checks', 0) >= 37, report.get('checks')
          print('M2 controller regression:', report.get('checks', 0), 'checks passed')
          PY

      - name: Run attack preview regression
        shell: bash
        run: |
          set -o pipefail
          rm -f docs/M2_ATTACK_PREVIEW_QA.json
          /tmp/godot/Godot_v4.6.3-stable_linux.x86_64 --headless --path . res://tests/night_attack_preview.tscn --quit-after 1800 2>&1 | tee /tmp/m2-preview-regression.log
          ! grep -E "SCRIPT ERROR|Parse Error|Compile Error" /tmp/m2-preview-regression.log
          test -s docs/M2_ATTACK_PREVIEW_QA.json
          python3 - <<'PY'
          import json
          report = json.load(open('docs/M2_ATTACK_PREVIEW_QA.json', encoding='utf-8'))
          assert report.get('failures') == [], report.get('failures')
          assert report.get('checks', 0) >= 35, report.get('checks')
          print('M2 attack preview regression:', report.get('checks', 0), 'checks passed')
          PY

      - name: Run expedition regression
        shell: bash
        run: |
          set -o pipefail
          rm -f docs/M1_QA.json
          /tmp/godot/Godot_v4.6.3-stable_linux.x86_64 --headless --path . res://tests/expedition.tscn --quit-after 1800 2>&1 | tee /tmp/m2-controller-preview-expedition.log
          ! grep -E "SCRIPT ERROR|Parse Error|Compile Error" /tmp/m2-controller-preview-expedition.log
          test -s docs/M1_QA.json
          python3 - <<'PY'
          import json
          report = json.load(open('docs/M1_QA.json', encoding='utf-8'))
          assert report.get('failures') == [], report.get('failures')
          print('M2 controller preview expedition regression:', report.get('checks', 0), 'checks passed')
          PY

      - name: Upload QA evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m2-controller-preview-qa
          path: |
            docs/M2_CONTROLLER_PREVIEW_QA.json
            docs/M2_CONTROLLER_QA.json
            docs/M2_ATTACK_PREVIEW_QA.json
            docs/M1_QA.json
            /tmp/m2-controller-preview-import.log
            /tmp/m2-controller-preview.log
            /tmp/m2-controller-regression.log
            /tmp/m2-preview-regression.log
            /tmp/m2-controller-preview-expedition.log
          if-no-files-found: ignore
